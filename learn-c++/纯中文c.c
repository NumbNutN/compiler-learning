#include <stdio.h>
#include <stdlib.h>

#define main 主要函数
#define printf 打印
#define return 返回


typedef void 未定型;
typedef int 整数;
typedef char 字符;

// 未定型 打印(字符* 格式化参数, ){

// }

整数 主要函数(){
    整数 甲 = 3;
    打印("%d",甲);
}